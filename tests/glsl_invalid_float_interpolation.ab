import "../src/shader/glsl.ab"

fun main: int {
    val scale = 0.5
    val shader = $glsl {
        compute {
            #version 450
            layout(local_size_x = ${float: scale}) in;
            void main() {}
        }
    }
    if (shader.valid) 1 else 42
}
